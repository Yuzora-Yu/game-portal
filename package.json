{
  "name": "yu-zora-game-portal",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node tools/build.mjs",
    "test": "npm run build",
    "deploy:cloudflare": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.120.0"
  }
}
