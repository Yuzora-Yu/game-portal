{
  "name": "yu-zora-game-portal",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node tools/build.mjs",
    "validate": "node tools/validate-structure.mjs",
    "test": "npm run validate && npm run build",
    "deploy:cloudflare": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.120.0"
  }
}
